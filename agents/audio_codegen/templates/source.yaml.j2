  # Generated by `agents.audio_codegen.spec_emitter` for source: {{ source_id }}
  # Operator merges this fragment into config/audio-topology.yaml's
  # `nodes:` array. The fragment is schema-v3-compliant; downstream
  # validation runs against the merged result.
  - id: {{ source_id }}
    kind: filter_chain
    pipewire_name: {{ pipewire_name }}
    description: "{{ description }}"
    target_object: null
    chain_kind: {{ chain_kind }}
    {%- if chain_kind in ("loudnorm", "usb-bias") %}
    limit_db: {{ limit_db }}
    release_s: {{ release_s }}
    {%- endif %}
    channels:
      count: 2
      positions: [FL, FR]
    params: {}
