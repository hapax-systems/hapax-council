"""World-surface health adapters.

Projects runtime evidence onto rows the operator can read via existing
world-surface dashboards. See ``audio_adapter`` for the audio attempt
adapter that pairs ``VoiceOutputRouter`` route results with
``broadcast_audio_health`` audibility evidence.
"""
