The native Tauri shell is decommissioned, so generated Tauri permission
schemas are no longer part of the active build surface.

The production control surface is the browser UI backed by `logos-api :8051`.
See `docs/runbooks/tauri-logos-decommission.md` for the runtime disposition and
security blocker status.
