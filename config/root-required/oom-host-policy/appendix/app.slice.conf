[Slice]
# Appendix aggregate backstop. This canonical profile remains below physical
# RAM while preserving recovery headroom outside app.slice. Derived here, not copied:
# app_max = uid_max - 2G and app_high = app_max - 8G, from the measured 60.38 GiB MemTotal
# (the derivation is written out in config/root-required/oom-host-profiles.tsv).
MemoryHigh=36G
MemoryMax=44G
MemorySwapMax=8G
MemoryLow=16G
MemoryMin=8G
