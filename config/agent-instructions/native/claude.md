## Claude binding

The global binding remains `CLAUDE.md` for compatibility and observable
InstructionsLoaded delivery. Repository `CLAUDE.md` may alias canonical
`AGENTS.md`; do not read both resolved bodies again.

Agent and Task are disabled. Dispatching plugins (`superpowers`, `feature-dev`,
`pr-review-toolkit`, `ralph-loop`) remain retired; do not re-enable them or
follow their stale auto-invocation guidance. Operator-invoked slash commands
are a separate capability and are not prohibited by that ruling.

Legacy gemini-cli worker/MCP availability must be checked against the current
capability registry; historical global delegation recipes do not admit it.
Use the declared agy or other admitted capability appropriate to the task.
Observe provider quota signals and report exhausted capacity without silently
switching billing surface. Existing native hooks and their receipt semantics
remain native additions; instruction consolidation does not change them.
