## Grok binding

The native global file is `$GROK_HOME/AGENTS.md` (default `~/.grok/AGENTS.md`).
Its complete rendered body must stay below Grok's 10,000-character file cap.
Project loading also depends on trust, Git boundaries and exclusions. Do not
claim that discovering the global file proves project delivery.

Use the admitted `hapax-grok-lane <role>` shape for a claiming lane; lane
identity and host placement come from that launch, not a global instruction.
Follow the applicable `hapax-workflow`/relay protocol. Native claude-compatible
hooks are disabled in the current estate shape; Grok conformance is
instruction-level. It is not equivalent to Claude/Codex hook activation.
The adapter and authorization validator are described in
`~/.grok/hooks/README-hapax-adhoc.md`; verify their actual availability.

Memory orientation: `~/.grok/memory/projects-hapax-sdlc-e2e.md`,
`~/.grok/memory/projects-hapax-system-geography.md`, and the canonical
`30-areas/hapax/system-model/` references. Preserve protected-path limits in
the admitted shape. Do not infer broader authority from an absent hook.
