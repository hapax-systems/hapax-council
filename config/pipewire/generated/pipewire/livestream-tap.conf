# Hapax livestream producer tap, upstream of broadcast master
context.objects = [
    {  factory = adapter
        args = {
            factory.name = support.null-audio-sink
            node.name    = "hapax-livestream-tap"
            media.class  = Audio/Sink
            audio.channels = 2
                audio.position = [ FL FR ]
        }
    }
]
