# Disabled notification-private monitor capture.
#
# Notifications remain fail-closed at hapax-notification-private and have no
# downstream capture/playback bridge until explicitly promoted.
context.modules = []
