# OBS-stable capture side for hapax-broadcast-normalized
context.modules = [
    {  name = libpipewire-module-filter-chain
        args = {
            node.description = "OBS-stable capture side for hapax-broadcast-normalized"
            audio.rate = 48000
            audio.channels = 2
            audio.position = [ FL FR ]
            capture.props = {
                node.name = "hapax-obs-broadcast-remap-capture"
            }
            playback.props = {
                node.name = "hapax-obs-broadcast-remap-capture-playback"
            target.object = "hapax-broadcast-normalized"
            }
        }
    }
]
