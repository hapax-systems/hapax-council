# Disabled notification-private monitor bridge.
#
# Notifications are fail-closed at hapax-notification-private. They must not
# share private TTS MPC AUX8/AUX9, L-12, OBS, default multimedia, or livestream
# egress until a bounded activation task specifies the route.
context.modules = []
