# PC-wide loudnorm sink, fail-closed with no hardware or broadcast egress
context.modules = [
    {  name = libpipewire-module-filter-chain
        args = {
            node.name = "hapax-pc-loudnorm"
            node.description = "PC-wide loudnorm sink, fail-closed with no hardware or broadcast egress"
            media.class = "Audio/Sink"
            audio.rate = 44100
            audio.channels = 2
            audio.position = [ FL FR ]

            filter.graph = {
                nodes = [
                    { type = ladspa
                      plugin = "fast_lookahead_limiter_1913"
                      label = "fastLookaheadLimiter"
                      name = "pc_loudnorm"
                      control = {
                          "Input gain (dB)" = 0.0
                          "Limit (dB)"      = -18.0
                          "Release time (s)" = 0.2
                      }
                    }
                ]
                inputs  = [ "pc_loudnorm:Input 1"  "pc_loudnorm:Input 2"  ]
                outputs = [ "pc_loudnorm:Output 1" "pc_loudnorm:Output 2" ]
            }

            capture.props = {
                node.name = "hapax-pc-loudnorm"
                media.class = "Audio/Sink"
            }
            playback.props = {
                node.name = "hapax-pc-loudnorm-playback"
                node.passive = false
                audio.channels = 2
                audio.position = [ FL FR ]

                node.autoconnect = false
                node.dont-fallback = true
                node.dont-reconnect = true
                node.dont-move = true
                stream.dont-remix = true
            }
        }
    }
]
