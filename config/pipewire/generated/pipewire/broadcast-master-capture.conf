# Livestream tap to broadcast master virtual source
context.modules = [
    {  name = libpipewire-module-filter-chain
        args = {
            node.description = "Livestream tap to broadcast master virtual source"
            audio.rate = 48000
            audio.channels = 2
            audio.position = [ FL FR ]
            capture.props = {
                node.name = "hapax-broadcast-master-capture"
            }
            playback.props = {
                node.name = "hapax-broadcast-master-capture-playback"
            target.object = "hapax-livestream-tap"
            }
        }
    }
]
