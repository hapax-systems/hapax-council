# Legacy application-facing livestream sink retained as a null sink
context.objects = [
    {  factory = adapter
        args = {
            factory.name = support.null-audio-sink
            node.name    = "hapax-livestream"
            media.class  = Audio/Sink
            audio.channels = 2
                audio.position = [ FL FR ]
        }
    }
]
