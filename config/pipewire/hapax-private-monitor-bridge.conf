# Hapax private monitor bridge — explicit off-L-12 hardware monitor.
#
# ``hapax-private`` and ``hapax-notification-private`` remain null sinks
# in their own config files. This file is the only audible private-monitor
# bridge: it captures those sink monitors and plays them to the Blue Yeti
# headphone jack, which is structurally outside the L-12 broadcast path.
#
# Fail-closed contract:
#   - target.object names the exact Yeti ALSA sink, never a default sink.
#   - absent hardware produces silence, not fallback.
#   - node.dont-fallback=true prevents WirePlumber from choosing L-12,
#     hapax-pc-loudnorm, Multimedia, or any other best/default target when
#     the Yeti is absent.
#   - node.dont-reconnect=true prevents an established bridge from moving
#     to another sink when the Yeti disappears.
#   - node.dont-move=true and state.restore=false keep pavucontrol or old
#     stream-state metadata from persisting a broad Loopback move.
#   - node.linger=true lets the stream wait silently for the Yeti instead
#     of falling through to default hardware.
#
# Source monitors are expressed with ``stream.capture.sink=true`` plus
# ``target.object=<sink>`` because that is the PipeWire/WirePlumber pattern
# used elsewhere in this repo for null-sink monitor capture.

context.modules = [
    {   name = libpipewire-module-loopback
        args = {
            node.description = "Hapax private monitor to Yeti headphone"
            capture.props = {
                node.name = "hapax-private-monitor-capture"
                stream.capture.sink = true
                target.object = "hapax-private"
                audio.channels = 2
                audio.position = [ FL FR ]
                stream.dont-remix = true
                state.restore = false
            }
            playback.props = {
                node.name = "hapax-private-playback"
                node.nick = "Hapax Private Yeti"
                node.description = "Hapax private monitor -> Blue Yeti headphone"
                target.object = "alsa_output.usb-Blue_Microphones_Yeti_Stereo_Microphone_REV8-00.analog-stereo"
                audio.channels = 2
                audio.position = [ FL FR ]
                stream.dont-remix = true
                node.dont-fallback = true
                node.dont-reconnect = true
                node.dont-move = true
                node.linger = true
                state.restore = false
            }
        }
    }

    {   name = libpipewire-module-loopback
        args = {
            node.description = "Hapax notification-private monitor to Yeti headphone"
            capture.props = {
                node.name = "hapax-notification-private-monitor-capture"
                stream.capture.sink = true
                target.object = "hapax-notification-private"
                audio.channels = 2
                audio.position = [ FL FR ]
                stream.dont-remix = true
                state.restore = false
            }
            playback.props = {
                node.name = "hapax-notification-private-playback"
                node.nick = "Hapax Notification Yeti"
                node.description = "Hapax notification-private monitor -> Blue Yeti headphone"
                target.object = "alsa_output.usb-Blue_Microphones_Yeti_Stereo_Microphone_REV8-00.analog-stereo"
                audio.channels = 2
                audio.position = [ FL FR ]
                stream.dont-remix = true
                node.dont-fallback = true
                node.dont-reconnect = true
                node.dont-move = true
                node.linger = true
                state.restore = false
            }
        }
    }
]
