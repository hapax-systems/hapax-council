# Load the SP5100/SB800 TCO hardware watchdog at boot (both AMD hosts:
# podium + appendix). Requires the un-denylist shadow file
# config/modprobe.d/blacklist.conf — systemd-modules-load refuses
# blacklisted modules, so this entry alone is inert (proven 2026-03→06 on
# podium). Armed by PID 1 via RuntimeWatchdogSec; rollback in
# docs/runbooks/hardware-watchdog-sp5100.md.
sp5100_tco
