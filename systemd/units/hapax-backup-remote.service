[Unit]
Description=Dump PostgreSQL/Qdrant/n8n, back up system state to B2, and publish the DR script
After=network-online.target
RequiresMountsFor=/store/llm-data
ConditionPathIsMountPoint=/store
OnFailure=notify-failure@%n.service

[Service]
Type=oneshot
ExecStart=%h/.cache/hapax/source-activation/worktree/scripts/hapax-backup-remote
WorkingDirectory=%h/.cache/hapax/source-activation/worktree
Environment=PATH=/home/hapax/.local/bin:/usr/local/bin:/usr/bin:/bin
Environment=HOME=/home/hapax
Environment=GNUPGHOME=/home/hapax/.gnupg
Environment=PASSWORD_STORE_DIR=/home/hapax/.password-store
MemoryMax=2G
CPUQuota=50%
SyslogIdentifier=hapax-backup-remote
