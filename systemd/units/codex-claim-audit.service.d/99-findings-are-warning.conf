[Service]
# codex-claim-audit exits 1 when unresolved claim/lane findings remain.
# Release preflight/reconciliation failures exit 2 and must still fail the unit.
# The script already emits ntfy warnings; keep user-unit failed state for
# execution failures, not for an audit result with findings.
SuccessExitStatus=1
