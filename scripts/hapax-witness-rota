#!/usr/bin/python3 -I
"""Witness pending pre-submission records (the system unit hapax-witness-rota@scan.service).

Installed to /usr/local/sbin by operator act O3. Isolated mode (-I) ignores PYTHON* variables,
the user site and the script's directory, so only the root-owned library copy below is imported.
"""

import sys

sys.path.insert(0, "/usr/local/lib/hapax/witness-rota")

from shared.witness_rota import main  # noqa: E402

sys.exit(main())
