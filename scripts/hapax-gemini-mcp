#!/usr/bin/env bash
set -euo pipefail

cat >&2 <<'EOF'
hapax-gemini-mcp is retired.

Codex must not install the legacy Gemini CLI MCP bridge. Use Context7, official
API routes, or agy-backed Hapax packet/review wrappers as appropriate.
EOF
exit 64
