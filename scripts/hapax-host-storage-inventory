#!/usr/bin/env python3
"""Thin CLI wrapper for shared.host_storage_inventory (host-scoped receipts)."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from shared.host_storage_inventory import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
